---
title: Lighting-grounded Video Generation with Renderer-based Agent Reasoning
title_zh: 基于光照引导及渲染器智能体推理的视频生成
authors: "Ziqi Cai, Taoyu Yang, Zheng Chang, Si Li, Han Jiang, Shuchen Weng, Boxin Shi"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.07966v1"
tags: ["query:cv-vg-cs"]
score: 8.0
evidence: 基于显式3D场景属性的可控视频生成
tldr: 针对视频生成中光照、布局和相机轨迹难以精确控制的问题，本文提出 LiVER 框架。通过引入包含 3D 场景属性标注的大规模数据集，LiVER 利用渲染器生成的控制信号引导扩散模型，实现了场景要素的解耦控制。此外，该框架还包含一个场景智能体，能将自然语言指令转化为 3D 控制信号，在保持高保真度和时序一致性的同时，显著提升了视频生成的交互性和可控性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.07966v1/fig-001.webp\", \"caption\": \"Figure 3. Pipeline of LiVER. Given a text prompt T , our Scene Agent parses object categories, spatial relations, and coarse geometry to construct an initial 3D scene. The Camera Agent infers a camera trajectory consistent with the described viewpoint and scene semantics, producing the camera condition C. The 3D scene is then rendered through a physically-based renderer to obtain the lighting-grounded scene proxy, including diffuse, glossy GGX, and rough GGX components, forming the scene condition Xcond. These components encode physically meaningful cues such as material response, shading, and reflections. The Lighting condition L, represented by an environment map, provides global illumination cues. The DiT-based video diffusion model integrates all conditions Xcond, C, L and generates a photorealistic video that preserves the scene layout, camera trajectory, and physically accurate lighting behavior.\", \"page\": 4, \"index\": 1, \"width\": 1026, \"height\": 352}, {\"url\": \"assets/figures/arxiv/2604.07966v1/fig-002.webp\", \"caption\": \"Figure 2. Our data annotation pipeline for LiVER-Real. We process each video to reconstruct its 3D geometry and estimate its HDR environment map. These are then used to render three pixel-aligned lighting representations (Diffuse, Glossy GGX, Rough GGX), which are concatenated to form the final conditioning input.\", \"page\": 3, \"index\": 2, \"width\": 973, \"height\": 290}, {\"url\": \"assets/figures/arxiv/2604.07966v1/fig-003.webp\", \"caption\": \"Figure 5. By manipulating the HDR environment map, our model produces continuous and physically consistent lighting variations. We show the diffuse, glossy, and rough GGX components of the scene proxy (top three rows) and the corresponding synthesized outputs (bottom). Lighting changes are reflected in shading and reflections while geometry and materials remain stable.\", \"page\": 8, \"index\": 3, \"width\": 1055, \"height\": 257}, {\"url\": \"assets/figures/arxiv/2604.07966v1/fig-004.webp\", \"caption\": \"Figure 6. Qualitative results of our ablation study.\", \"page\": 8, \"index\": 4, \"width\": 496, \"height\": 299}]"
motivation: 现有的视频扩散模型在布局、光照和相机轨迹等关键场景要素上缺乏精确且解耦的控制能力，限制了其在专业影视制作中的应用。
method: 提出一种基于渲染器的扩散框架，利用 3D 场景属性作为显式条件信号，并通过轻量化模块和场景智能体实现从自然语言指令到 3D 控制信号的自动化转换。
result: 实验证明 LiVER 在照片级真实感和时序一致性方面达到了 SOTA 水平，并实现了对光照、布局等场景要素的精确解耦控制。
conclusion: LiVER 通过结合 3D 表征与扩散模型，为可控视频生成设立了新标准，有效解决了复杂场景编辑与高层指令推理的难题。
---

## 摘要
扩散模型在视频生成方面取得了显著进展，但其可控性仍是一个主要局限。布局、光照和摄像机轨迹等关键场景要素往往相互纠缠或建模较弱，限制了它们在电影制作和虚拟生产等需要显式场景控制的领域中的应用。我们提出了 LiVER，这是一个用于场景可控视频生成的扩散框架。为实现这一目标，我们引入了一个新颖的框架，将视频合成建立在显式 3D 场景属性之上，并辅以一个包含物体布局、光照和摄像机参数密集标注的新型大规模数据集。我们的方法通过从统一的 3D 表示中渲染控制信号来解耦这些属性。我们提出了一个轻量级条件模块和一种渐进式训练策略，将这些信号集成到基础视频扩散模型中，确保了稳定的收敛和高保真度。我们的框架支持广泛的应用，包括底层 3D 场景完全可编辑的图像到视频和视频到视频合成。为了进一步增强易用性，我们开发了一个场景智能体，能够自动将高层用户指令转换为所需的 3D 控制信号。实验表明，LiVER 在实现精确、解耦的场景要素控制的同时，达到了最先进的照片级真实感和时间一致性，为可控视频生成树立了新标准。

## Abstract
Diffusion models have achieved remarkable progress in video generation, but their controllability remains a major limitation. Key scene factors such as layout, lighting, and camera trajectory are often entangled or only weakly modeled, restricting their applicability in domains like filmmaking and virtual production where explicit scene control is essential. We present LiVER, a diffusion-based framework for scene-controllable video generation. To achieve this, we introduce a novel framework that conditions video synthesis on explicit 3D scene properties, supported by a new large-scale dataset with dense annotations of object layout, lighting, and camera parameters. Our method disentangles these properties by rendering control signals from a unified 3D representation. We propose a lightweight conditioning module and a progressive training strategy to integrate these signals into a foundational video diffusion model, ensuring stable convergence and high fidelity. Our framework enables a wide range of applications, including image-to-video and video-to-video synthesis where the underlying 3D scene is fully editable. To further enhance usability, we develop a scene agent that automatically translates high-level user instructions into the required 3D control signals. Experiments show that LiVER achieves state-of-the-art photorealism and temporal consistency while enabling precise, disentangled control over scene factors, setting a new standard for controllable video generation.

---

## 论文详细总结（自动生成）

这是一份关于论文《Lighting-grounded Video Generation with Renderer-based Agent Reasoning》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的视频生成模型（如 Sora、Wan2.1 等）虽然能生成高质量视频，但在**物理可控性**上存在严重缺陷。特别是光照（Lighting）、场景布局（Layout）和相机轨迹（Camera Trajectory）这三个关键要素往往相互纠缠，难以进行精确且独立的编辑。
*   **研究背景**：在电影制作和虚拟生产中，导演需要对光照效果（如阴影、反射、环境遮挡）进行物理级别的精确控制。现有的数据驱动方法难以模拟复杂的物理交互（如 BRDF 材质表现），导致生成的视频在金属、玻璃或皮肤等材质上的光照表现不真实。

### 2. 方法论
LiVER 框架的核心思想是将 **3D 物理渲染信号** 作为扩散模型的显式约束，并通过 **LLM 智能体** 简化用户操作。
*   **核心组件**：
    1.  **渲染器智能体推理 (Renderer-based Agent Reasoning)**：利用大语言模型（LLM）作为智能体，将用户的自然语言指令解析为结构化的场景图（Scene Graph），自动检索 3D 资产、规划相机轨迹并配置 HDR 环境贴图。
    2.  **光照引导场景代理 (Lighting-grounded Scene Proxy)**：不直接使用复杂的 3D 表示，而是通过 3D 引擎（Blender）渲染出一组 2D 渲染通道（Render Passes），包括：
        *   **Diffuse（漫反射）**：捕捉低频环境光。
        *   **Rough GGX（粗糙反射）**：捕捉中频反射。
        *   **Glossy GGX（光泽反射）**：捕捉高频镜面高光。
    3.  **条件注入网络**：设计了一个轻量级的条件编码器（Eproxy）和适配器，将上述 9 通道的场景代理信号（F帧 × 9通道）对齐并叠加到预训练视频模型（Wan2.2-5B）的潜空间中。
*   **训练策略**：采用**三阶段渐进式训练**：
    1.  仅训练条件路径（冻结主干）。
    2.  联合 LoRA 微调（优化语义对齐）。
    3.  光照多样性扩展（混合真实与合成数据）。

### 3. 实验设计
*   **数据集 (LiVERSet)**：
    *   **LiVER-Real**：包含 5.5K 段真实视频，通过算法反求相机位姿、深度图和 HDR 环境贴图。
    *   **LiVER-Syn**：包含 5.5K 段合成视频，利用 Objaverse-XL 的 PBR 材质资产，在 Blender 中进行物理精确渲染，支持动态光照（如环境贴图旋转）。
*   **Benchmark 与对比方法**：
    *   对比了 **CameraCtrl**（相机控制）、**MotionCtrl**（相机与物体运动控制）和 **VideoFrom3D**（基于 3D 几何生成视频）。
    *   评估指标包括：图像质量（FVD, FID）、语义一致性（CLIP）、控制精度（ATE 相机误差、SI-MSE 光照误差、mIoU 布局误差）。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA H100 GPU**。
*   **训练细节**：
    *   总训练步数约为 **100K steps**。
    *   每张 GPU 的 Batch Size 为 2（总 Batch Size 为 16）。
    *   学习率固定为 $1 \times 10^{-5}$，使用 AdamW 优化器。
    *   生成分辨率为 $704 \times 1280$。

### 5. 实验数量与充分性
*   **实验规模**：论文在 1K 段保留测试集上进行了定量评估，并进行了 25 人的用户主观研究（User Study）。
*   **消融实验**：针对“合成数据的作用”和“阶段性训练策略”进行了对比实验。
*   **充分性评价**：实验设计较为全面，涵盖了从底层物理指标（位姿、光照误差）到高层感知指标（FVD、用户偏好）的多个维度。对比基线选择了当前最相关的 SOTA 模型，实验结果具有较强的说服力。

### 6. 主要结论与发现
*   **物理引导的优越性**：引入基于物理渲染的代理信号（如 GGX 反射通道）能显著提升视频中材质表现的真实感，解决光影闪烁和不匹配问题。
*   **解耦控制的实现**：LiVER 成功实现了在保持几何结构不变的情况下，仅通过更换 HDR 环境贴图来改变视频光照。
*   **合成数据的价值**：实验证明，仅靠真实数据难以让模型学会复杂的光照控制，LiVER-Syn 合成数据集提供的多样化光照标签是实现精确控制的关键。

### 7. 优点与亮点
*   **创新的代理表示**：将复杂的 3D 物理属性简化为像素对齐的 2D 渲染通道，既保留了物理准确性，又降低了扩散模型的学习难度。
*   **全流程自动化**：通过 Agent Reasoning 实现了从“文字描述”到“3D 场景构建”再到“视频生成”的闭环，极大地降低了专业视频制作的门槛。
*   **高质量数据集**：贡献了一个带有密集物理标注（光照、位姿、材质）的大规模视频数据集，对社区具有参考价值。

### 8. 不足与局限
*   **几何细节依赖 Prompt**：由于初始 3D 重建较为粗糙，精细的几何纹理和材质细节仍高度依赖文本描述，如果 Prompt 描述不足，生成质量会下降。
*   **计算开销**：虽然推理时使用了轻量级模块，但生成过程仍依赖大型视频扩散模型，且需要 3D 引擎预渲染代理信号，实时性受限。
*   **资产库限制**：Agent 的表现受限于预定义的 3D 资产库（Objaverse-XL），对于库中不存在的罕见物体，生成效果可能打折扣。

（完）

---
title: "MMControl: Unified Multi-Modal Control for Joint Audio-Video Generation"
title_zh: MMControl：音视频联合生成的统一多模态控制
authors: "Liyang Li, Wen Wang, Canyu Zhao, Tianjian Feng, Zhiyue Zhao, Hao Chen, Chunhua Shen"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19679v2"
tags: ["query:cv-vg-cs"]
score: 8.0
evidence: 多模态信号引导的可控音视频联合生成
tldr: 针对现有音视频联合生成模型缺乏多模态控制的问题，本文提出 MMControl 框架。该框架通过双流条件注入机制，将参考图像、音频、深度图和姿态序列等视觉与听觉信号引入联合扩散 Transformer（DiT）中。MMControl 实现了角色身份、音色及结构约束的同步控制，并支持独立调节各模态影响强度，显著提升了音视频生成的一致性与可控性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.19679v2/fig-001.webp\", \"caption\": \"Figure 3: Two-Stage Progressive Inference Strategy. Stage 1 generates a low-resolution (h/2× w/2) semantic base using modality-specific scaling factors (γv, γa) to independently modulate visual and acoustic guidance. Stage 2 employs a distilled LoRA to refine fine-grained textures and upscale the output to the target full resolution (h× w).\", \"page\": 6, \"index\": 1, \"width\": 768, \"height\": 287}, {\"url\": \"assets/figures/arxiv/2604.19679v2/fig-002.webp\", \"caption\": \"Figure 6: Our method exhibits superior adherence to both structural pose signals and text prompts. Compared to baselines, MMControl generates videos with higher visual quality, more accurate character identity, and precise audio-visual synchronization.\", \"page\": 11, \"index\": 2, \"width\": 818, \"height\": 648}, {\"url\": \"assets/figures/arxiv/2604.19679v2/fig-003.webp\", \"caption\": \"Figure 1: Unified Multi-Modal Control results produced by MMControl. By providing different control signals, our model enables fine-grained control over joint audio-video synthesis. The teaser illustrates our model’s ability to generate content that is: Consistent in voice and identity (via audio/visual references) and Controllable in structure and motion (via depth/pose guidance).\", \"page\": 2, \"index\": 3, \"width\": 816, \"height\": 544}, {\"url\": \"assets/figures/arxiv/2604.19679v2/fig-004.webp\", \"caption\": \"Figure 5: Qualitative comparison of depth control. The visual quality of images from VideoComposer and ControlVideo is lower. VACE fails to accurately follow the depth signal(e.g., the hand gestures). Our method provides high visual quality and precise control adherence.\", \"page\": 10, \"index\": 4, \"width\": 821, \"height\": 655}, {\"url\": \"assets/figures/arxiv/2604.19679v2/fig-005.webp\", \"caption\": \"Figure 4: Qualitative comparison with baseline methods. As shown, baseline approaches such as HunyuanCustom, SadTalker, Hallo3 and AniPortrait often produce noticeable facial artifacts and largely static backgrounds, which limits scene expressiveness. MMControl generates high-fidelity videos with richer motion and natural transitions while maintaining stable character ID and precise audio-visual synchronization.\", \"page\": 9, \"index\": 5, \"width\": 813, \"height\": 618}, {\"url\": \"assets/figures/arxiv/2604.19679v2/fig-006.webp\", \"caption\": \"Figure 2: Proposed Dual-Stream Bypass architecture for Joint DiT. Our proposed Dual-Stream Bypass mechanism introduces parallel, trainable visual and acoustic bypass branches interleaved into the frozen Joint DiT backbone at even-numbered layers.\", \"page\": 5, \"index\": 6, \"width\": 806, \"height\": 361}]"
motivation: 现有的音视频生成控制框架多局限于视频维度，导致跨模态对齐效果不佳且缺乏全面的多模态控制能力。
method: 引入双流条件注入机制，通过旁路分支将视觉和听觉控制信号注入联合音视频 DiT，并结合模态特定的引导缩放技术。
result: 实验证明该方法能在保持角色身份和音色一致性的同时，实现对身体姿态和场景布局的精细化、可组合控制。
conclusion: MMControl 为音视频联合生成提供了统一且灵活的多模态控制方案，有效增强了生成内容的表现力和一致性。
---

## 摘要
扩散 Transformer (DiTs) 的最新进展已实现了高质量的音视频联合生成，能够在单一模型中生成带有同步音频的视频。然而，现有的可控生成框架通常局限于仅限视频的控制。这限制了全面的可控性，并往往导致跨模态对齐效果欠佳。为弥补这一差距，我们提出了 MMControl，使用户能够在音视频联合生成中进行多模态控制。MMControl 引入了一种双流条件注入机制，将视觉和声学控制信号（包括参考图像、参考音频、深度图和姿态序列）整合到联合生成过程中。这些条件通过旁路分支注入到音视频联合扩散 Transformer 中，使模型能够在结构约束下同时生成身份一致的视频和音色一致的音频。此外，我们引入了特定模态的引导缩放，允许用户在推理阶段独立且动态地调整每个视觉和声学条件的影响强度。大量实验表明，MMControl 在音视频联合生成中实现了对角色身份、语音音色、身体姿态和场景布局的细粒度、可组合控制。

## Abstract
Recent advances in Diffusion Transformers (DiTs) have enabled high-quality joint audio-video generation, producing videos with synchronized audio within a single model. However, existing controllable generation frameworks are typically restricted to video-only control. This restricts comprehensive controllability and often leads to suboptimal cross-modal alignment. To bridge this gap, we present MMControl, which enables users to perform Multi-Modal Control in joint audio-video generation. MMControl introduces a dual-stream conditional injection mechanism. It incorporates both visual and acoustic control signals, including reference images, reference audio, depth maps, and pose sequences, into a joint generation process. These conditions are injected through bypass branches into a joint audio-video Diffusion Transformer, enabling the model to simultaneously generate identity-consistent video and timbre-consistent audio under structural constraints. Furthermore, we introduce modality-specific guidance scaling, which allows users to independently and dynamically adjust the influence strength of each visual and acoustic condition at inference time. Extensive experiments demonstrate that MMControl achieves fine-grained, composable control over character identity, voice timbre, body pose, and scene layout in joint audio-video generation.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **MMControl** 的新型框架，旨在解决音视频联合生成（Joint Audio-Video Generation）中的多模态精细化控制问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：虽然扩散 Transformer (DiT) 在音视频同步生成上取得了突破，但现有的可控生成方法大多是“单模态偏向”的（要么只控视频，要么只控音频）。这导致在需要同时控制角色身份（视觉）、音色（听觉）和结构（如姿态、深度）时，模型难以保持跨模态的高度对齐和一致性。
*   **研究背景**：传统的级联式（先视频后音频或反之）管线容易产生不自然的效果。最新的联合生成模型（如 LTX-2）虽然能同时生成音视频，但缺乏像 ControlNet 那样的精细控制手段。

### 2. 方法论：核心思想与关键技术
MMControl 的核心在于将多种异构信号统一到一个生成框架中，主要包含以下三个关键组件：
*   **多模态控制单元 (MMCU)**：这是一个统一的接口，负责将参考图像、参考音频、深度图和姿态序列等不同格式的输入进行 Token 化，并利用掩码（Mask）机制将“参考信息”与“生成约束”在时间维度上进行对齐。
*   **双流旁路架构 (Dual-Stream Bypass)**：
    *   为了不破坏预训练联合 DiT 模型（如 LTX-2）的生成能力，设计了两个平行的可训练分支（视觉旁路和听觉旁路）。
    *   这些分支通过“门控残差连接”将控制特征注入到冻结的骨干网络中。
    *   旁路分支内部采用了自注意力、文本交叉注意力和前馈网络，但去除了显式的音视频交叉注意力以提高效率。
*   **模态特定引导缩放 (Modality-Specific Guidance Scaling)**：在推理阶段，引入了两个独立的缩放因子 $\gamma_v$（视觉）和 $\gamma_a$（听觉）。用户可以独立调节视觉结构和听觉音色的控制强度，从而在“严格遵循参考”和“自由创作”之间取得平衡。
*   **两阶段推理策略**：第一阶段生成低分辨率的语义底稿，第二阶段利用蒸馏 LoRA 进行细节增强和超分辨率提升。

### 3. 实验设计
*   **数据集**：从 Hallo3 数据集中筛选了 30,000 个高质量样本进行训练。
*   **任务场景**：涵盖了“参考图像+音频”、“参考图像+深度图”、“参考图像+姿态序列”等多种组合控制场景。
*   **Benchmark 与对比方法**：
    *   **音视频同步对比**：与 SadTalker、AniPortrait、HunyuanCustom、Hallo3 等音频驱动方法对比。
    *   **结构控制对比**：与 VideoComposer、ControlVideo、VACE 等视频控制方法对比。
    *   **评价指标**：VBench（视频质量）、Sync-C/D（音视频同步性）、CLIP/DINO（语义/身份一致性）、MAE（深度图结构保真度）。

### 4. 资源与算力
*   **硬件**：使用了 **4 台 NVIDIA H200 GPU**。
*   **训练细节**：
    *   骨干网络为 LTX-2 (19B 参数)。
    *   训练步数为 7,200 步，总时长约为 **12 小时**。
    *   使用了 AdamW 优化器，峰值学习率为 $1 \times 10^{-5}$。

### 5. 实验数量与充分性
*   **实验规模**：
    *   构建了包含 200 个样本的音视频同步测试集和 200 个样本的深度控制测试集。
    *   进行了详细的消融实验，验证了模态缩放因子（$\gamma_v, \gamma_a$）对生成结果的影响。
    *   开展了人工评估（User Study），邀请 20 名参与者对口型准确度、表情真实度、动作自然度等 6 个维度进行打分。
*   **充分性评价**：实验设计较为全面，不仅涵盖了客观指标，还通过主观评估弥补了生成模型评价的局限性。对比方法包含了当前最先进的（SOTA）开源模型，实验结果具有较强的说服力。

### 6. 主要结论与发现
*   **多模态协同**：MMControl 能够同时实现角色身份、音色和复杂动作（如手势、姿态）的精准控制，且音视频对齐效果优于现有的级联或单模态控制模型。
*   **性能领先**：在深度控制任务中，其 MAE 指标显著低于 VideoComposer 和 VACE；在音视频同步性（Sync-C）上，即使在不使用真实音频作为输入的情况下，也达到了领先水平。
*   **灵活性**：通过调节缩放因子，模型展现了极强的解耦控制能力（例如：只换音色不换脸，或只换脸不换音色）。

### 7. 优点与亮点
*   **统一性**：首次在联合音视频生成框架下实现了视觉和听觉信号的统一控制。
*   **非侵入式设计**：采用旁路分支（Bypass）注入控制信号，有效保留了预训练大模型的先验知识，且训练效率高（仅需 12 小时）。
*   **解耦控制**：引入的引导缩放机制为用户提供了极高的创作自由度，能够独立调节不同模态的干预程度。

### 8. 不足与局限
*   **场景限制**：目前主要集中在单人肖像或简单场景，对于多角色交互、复杂对话场景的跨人同步处理仍有待探索。
*   **泛化风险**：虽然在特定数据集上表现优异，但对于极端罕见的语音风格或复杂的背景环境，可能仍存在泛化能力不足的问题。
*   **长视频一致性**：论文主要展示了短片段生成，超长视频的长期时间相干性和逻辑一致性仍是未来挑战。

（完）

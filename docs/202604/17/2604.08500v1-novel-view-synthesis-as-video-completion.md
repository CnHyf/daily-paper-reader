---
title: Novel View Synthesis as Video Completion
title_zh: 将新视角合成视为视频补全
authors: "Qi Wu, Khiem Vuong, Minsik Jeon, Srinivasa Narasimhan, Deva Ramanan"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08500v1"
tags: ["query:cv-vg-cs"]
score: 6.0
evidence: 利用视频扩散模型进行新视角合成
tldr: 本研究针对稀疏新视图合成（NVS）挑战，提出将该任务视为低帧率视频补全过程。由于传统图像扩散模型缺乏多视图一致性，作者开发了 FrameCrafter，利用视频扩散模型中蕴含的隐式多视图知识。通过移除时间位置编码等架构改进，使模型对输入视图顺序具有不变性，从而在仅有少量输入图像的情况下，实现了高质量的目标视图预测。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.08500v1/fig-001.webp\", \"caption\": \"Fig. 4: Qualitative comparison under sparse inputs. FrameCrafter produces more geometrically consistent and sharper novel views compared to SEVA [55], Aether [57], LVSM [17], with improved viewpoint alignment and detail preservation.\", \"page\": 11, \"index\": 1, \"width\": 712, \"height\": 693}, {\"url\": \"assets/figures/arxiv/2604.08500v1/fig-002.webp\", \"caption\": \"Fig. 6: Additional Qualitative Comparisons with SEVA [55], Aether [57] and LVSM [17].\", \"page\": 21, \"index\": 2, \"width\": 714, \"height\": 697}, {\"url\": \"assets/figures/arxiv/2604.08500v1/fig-003.webp\", \"caption\": \"Fig. 2: Each input image is encoded by a frozen video VAE, and its camera pose is converted into a Plücker ray map that is concatenated with the image latent along the channel dimension. The resulting view tokens are then concatenated along the temporal dimension, where the first K tokens correspond to input views and the final token represents the query view. Only the patch embedding layer and the LoRA modules in the DiT backbone are trained. During both training and inference, only the predicted query latent is used for decoding and supervision.\", \"page\": 5, \"index\": 3, \"width\": 711, \"height\": 333}]"
motivation: 传统的图像扩散模型在处理稀疏视图合成时缺乏多视图一致性，而视频模型虽有此类知识但受限于严格的帧顺序要求。
method: 提出 FrameCrafter 架构，通过移除时间位置编码和引入逐帧潜变量编码，使视频模型能够处理无序的稀疏输入视图并实现置换不变性。
result: 实验证明，经过微调的视频模型能够有效“忘记”时间顺序，并在多个稀疏视图合成基准测试中表现出具有竞争力的性能。
conclusion: 将稀疏视图合成重新定义为视频补全任务，并通过简单的架构调整利用视频预训练模型的强大先验，是解决多视图合成问题的有效途径。
---

## 摘要
我们利用视频扩散模型解决稀疏新视角合成（NVS）问题；在给定场景的 $K$（约 5 张）多视图图像及其相机位姿的情况下，我们预测目标相机位姿下的视图。许多先前的方法利用了通过扩散模型编码的生成式图像先验。然而，在单张图像上训练的模型缺乏多视图知识。与此不同，我们认为视频模型已经包含了隐式的多视图知识，因此应该更容易适配于 NVS。我们的核心见解是将稀疏 NVS 建模为低帧率视频补全任务。然而，一个挑战在于稀疏 NVS 是定义在无序输入集上的，这些输入通常过于稀疏，难以形成有意义的顺序，因此模型应当对该输入集的排列具有不变性。为此，我们提出了 FrameCrafter，它通过包括逐帧潜变量编码和移除时间位置嵌入在内的若干架构修改，将视频模型（通常在连贯的帧序列上训练）适配于排列不变的 NVS。我们的结果表明，通过极少的监督，视频模型可以很容易地被训练成“忘记”时间，从而在稀疏视角 NVS 基准测试中产生具有竞争力的性能。项目页面：https://frame-crafter.github.io/

## Abstract
We tackle the problem of sparse novel view synthesis (NVS) using video diffusion models; given $K$ ($\approx 5$) multi-view images of a scene and their camera poses, we predict the view from a target camera pose. Many prior approaches leverage generative image priors encoded via diffusion models. However, models trained on single images lack multi-view knowledge. We instead argue that video models already contain implicit multi-view knowledge and so should be easier to adapt for NVS. Our key insight is to formulate sparse NVS as a low frame-rate video completion task. However, one challenge is that sparse NVS is defined over an unordered set of inputs, often too sparse to admit a meaningful order, so the models should be $\textit{invariant}$ to permutations of that input set. To this end, we present FrameCrafter, which adapts video models (naturally trained with coherent frame orderings) to permutation-invariant NVS through several architectural modifications, including per-frame latent encodings and removal of temporal positional embeddings. Our results suggest that video models can be easily trained to "forget" about time with minimal supervision, producing competitive performance on sparse-view NVS benchmarks. Project page: https://frame-crafter.github.io/
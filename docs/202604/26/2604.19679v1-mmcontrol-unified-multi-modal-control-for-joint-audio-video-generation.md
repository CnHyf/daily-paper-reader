---
title: "MMControl: Unified Multi-Modal Control for Joint Audio-Video Generation"
title_zh: MMControl：音视频联合生成的统一多模态控制
authors: "Liyang Li, Wen Wang, Canyu Zhao, Tianjian Feng, Zhiyue Zhao, Hao Chen, Chunhua Shen"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19679v1"
tags: ["query:cv-vg-cs"]
score: 8.0
evidence: 音视频联合生成的统一多模态控制
tldr: 针对现有音视频联合生成模型缺乏多模态控制的问题，本文提出MMControl框架。该框架通过双流条件注入机制，将参考图像、音频、深度图及姿态序列等视觉和听觉信号引入联合扩散Transformer（DiT）中。MMControl不仅实现了身份和音色的高度一致性，还支持独立调节各模态控制强度，为音视频同步生成提供了精细且可组合的控制能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.19679v1/fig-001.webp\", \"caption\": \"Figure 3: Two-Stage Progressive Inference Strategy. Stage 1 generates a low-resolution (h/2× w/2) semantic base using modality-specific scaling factors (γv, γa) to independently modulate visual and acoustic guidance. Stage 2 employs a distilled LoRA to refine fine-grained textures and upscale the output to the target full resolution (h× w).\", \"page\": 6, \"index\": 1, \"width\": 768, \"height\": 287}, {\"url\": \"assets/figures/arxiv/2604.19679v1/fig-002.webp\", \"caption\": \"Figure 6: Our method exhibits superior adherence to both structural pose signals and text prompts. Compared to baselines, MMControl generates videos with higher visual quality, more accurate character identity, and precise audio-visual synchronization.\", \"page\": 11, \"index\": 2, \"width\": 818, \"height\": 648}, {\"url\": \"assets/figures/arxiv/2604.19679v1/fig-003.webp\", \"caption\": \"Figure 1: Unified Multi-Modal Control results produced by MMControl. By providing different control signals, our model enables fine-grained control over joint audio-video synthesis. The teaser illustrates our model’s ability to generate content that is: Consistent in voice and identity (via audio/visual references) and Controllable in structure and motion (via depth/pose guidance).\", \"page\": 2, \"index\": 3, \"width\": 816, \"height\": 544}, {\"url\": \"assets/figures/arxiv/2604.19679v1/fig-004.webp\", \"caption\": \"Figure 5: Qualitative comparison of depth control. The visual quality of images from VideoComposer and ControlVideo is lower. VACE fails to accurately follow the depth signal(e.g., the hand gestures). Our method provides high visual quality and precise control adherence.\", \"page\": 10, \"index\": 4, \"width\": 821, \"height\": 655}, {\"url\": \"assets/figures/arxiv/2604.19679v1/fig-005.webp\", \"caption\": \"Figure 4: Qualitative comparison with baseline methods. As shown, baseline approaches such as HunyuanCustom, SadTalker, Hallo3 and AniPortrait often produce noticeable facial artifacts and largely static backgrounds, which limits scene expressiveness. MMControl generates high-fidelity videos with richer motion and natural transitions while maintaining stable character ID and precise audio-visual synchronization.\", \"page\": 9, \"index\": 5, \"width\": 813, \"height\": 618}, {\"url\": \"assets/figures/arxiv/2604.19679v1/fig-006.webp\", \"caption\": \"Figure 2: Proposed Dual-Stream Bypass architecture for Joint DiT. Our proposed Dual-Stream Bypass mechanism introduces parallel, trainable visual and acoustic bypass branches interleaved into the frozen Joint DiT backbone at even-numbered layers.\", \"page\": 5, \"index\": 6, \"width\": 806, \"height\": 361}]"
motivation: 现有的音视频生成控制框架多局限于视频维度，导致跨模态对齐效果不佳且缺乏全面的多模态控制手段。
method: 引入双流条件注入机制和旁路分支，将多模态控制信号整合进联合DiT模型，并采用模态特定的引导缩放技术进行动态调节。
result: 实验证明该方法能在保持角色身份和音色一致性的同时，实现对动作姿态和场景布局的精准、可组合控制。
conclusion: MMControl为音视频联合生成提供了统一且灵活的多模态控制方案，显著提升了生成内容的跨模态一致性与用户可控性。
---

## 摘要
扩散 Transformer (DiTs) 的最新进展已实现了高质量的音视频联合生成，能够在单一模型中生成带有同步音频的视频。然而，现有的可控生成框架通常局限于仅限视频的控制。这限制了全面的可控性，并往往导致跨模态对齐效果不佳。为了弥补这一差距，我们提出了 MMControl，使用户能够在音视频联合生成中进行多模态控制。MMControl 引入了一种双流条件注入机制，将视觉和声学控制信号（包括参考图像、参考音频、深度图和姿态序列）整合到联合生成过程中。这些条件通过旁路分支注入到音视频联合扩散 Transformer 中，使模型能够在结构约束下同时生成身份一致的视频和音色一致的音频。此外，我们引入了特定模态的引导缩放，允许用户在推理阶段独立且动态地调整每个视觉和声学条件的影响强度。大量实验表明，MMControl 在音视频联合生成中实现了对角色身份、语音音色、身体姿态和场景布局的细粒度、可组合控制。

## Abstract
Recent advances in Diffusion Transformers (DiTs) have enabled high-quality joint audio-video generation, producing videos with synchronized audio within a single model. However, existing controllable generation frameworks are typically restricted to video-only control. This restricts comprehensive controllability and often leads to suboptimal cross-modal alignment. To bridge this gap, we present MMControl, which enables users to perform Multi-Modal Control in joint audio-video generation. MMControl introduces a dual-stream conditional injection mechanism. It incorporates both visual and acoustic control signals, including reference images, reference audio, depth maps, and pose sequences, into a joint generation process. These conditions are injected through bypass branches into a joint audio-video Diffusion Transformer, enabling the model to simultaneously generate identity-consistent video and timbre-consistent audio under structural constraints. Furthermore, we introduce modality-specific guidance scaling, which allows users to independently and dynamically adjust the influence strength of each visual and acoustic condition at inference time. Extensive experiments demonstrate that MMControl achieves fine-grained, composable control over character identity, voice timbre, body pose, and scene layout in joint audio-video generation.

---

## 论文详细总结（自动生成）

这是一份关于论文《MMControl: Unified Multi-Modal Control for Joint Audio-Video Generation》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着扩散 Transformer (DiT) 的发展，音视频联合生成（Joint Audio-Video Generation）已能实现极高的同步性和质量。然而，现有的可控生成研究大多是“单模态中心化”的：要么侧重于视频控制（如深度、姿态），要么侧重于音频控制（如语音驱动口型），缺乏一个**统一的框架**来同时操纵视觉身份、听觉音色以及空间结构。这导致在复杂场景下，跨模态对齐（如特定人的声音与特定动作的配合）难以达到理想效果。本文旨在填补这一空白，提出一种能同时处理多种异构控制信号的统一控制方案。

### 2. 方法论：核心思想与关键技术
MMControl 的核心在于将多种控制信号整合进一个预训练好的、冻结的音视频联合扩散模型中。

*   **多模态控制单元 (MMCU)：** 这是一个统一的接口，负责将异构输入（参考图像、参考音频、深度图序列、姿态序列）转化为同步的 Token 表示。它通过掩码（Mask）机制区分“参考内容”和“生成约束”，确保模型在遵循结构约束的同时保持身份一致性。
*   **双流旁路架构 (Dual-Stream Bypass Architecture)：** 为了不破坏预训练模型的生成能力，设计了并行的视觉和听觉可训练分支。这些分支与冻结的联合 DiT 层交织，通过门控残差连接（Gated Residual Connections）将控制特征注入主干网络。
*   **模态特定引导缩放 (Modality-Specific Guidance Scaling)：** 在推理阶段，引入了独立的缩放因子 $\gamma_v$（视觉）和 $\gamma_a$（听觉）。用户可以动态调整两者的强度，例如在保持严格姿态对齐的同时，允许音频有更多的表现力。
*   **两阶段推理策略：** 
    1.  **语义基础生成：** 在低分辨率下生成音视频草图，确定全局结构。
    2.  **细节精细化：** 利用蒸馏 LoRA 和全分辨率控制信号进行上采样和纹理修复。

### 3. 实验设计
*   **数据集：** 从 Hallo3 数据集中筛选了 30,000 个高质量样本进行训练。
*   **任务场景：** 涵盖了“图像+音频”驱动、“图像+深度图”驱动、“图像+姿态”驱动等多种组合。
*   **Benchmark 与对比方法：**
    *   **音视频同步对比：** 与 SadTalker、AniPortrait、HunyuanCustom、Hallo3 等音频驱动模型对比。
    *   **结构控制对比：** 与 VideoComposer、ControlVideo、VACE 等视频控制模型对比。
    *   **评价指标：** 使用 Sync-C/D（同步性）、Text CLIP（语义一致性）、Subject DINO（身份保持）、MAE（深度图误差）以及 VBench 综合指标。

### 4. 资源与算力
*   **硬件：** 使用了 **4 台 NVIDIA H200 GPU**。
*   **训练细节：** 
    *   初始化自 LTX-2 (19B) 预训练模型。
    *   训练步数为 7,200 步，Batch Size 为 2（每张卡），并使用 2 步梯度累积。
    *   **训练时长：** 约 **12 小时**。这表明基于旁路分支的微调方案具有极高的效率。

### 5. 实验数量与充分性
*   **实验规模：** 构建了包含 400 个样本的评估基准（200 个用于音频驱动，200 个用于深度控制）。
*   **消融与定性研究：** 进行了针对 $\gamma_v$ 和 $\gamma_a$ 的解耦控制实验，验证了各模态控制的独立性。
*   **人类评估：** 招募了 20 名参与者对口型准确度、表情真实度、动作自然度等 6 个维度进行打分。
*   **充分性评价：** 实验设计较为全面，涵盖了定量指标、定性对比及主观评价。对比对象包含了最新的 SOTA 模型（如 VACE, Hallo3），评估过程相对客观公平。

### 6. 主要结论与发现
*   **统一控制的优越性：** MMControl 在保持角色身份（视觉）和音色（听觉）一致性方面显著优于传统的级联或单模态模型。
*   **高精度对齐：** 在深度控制任务中，其 MAE 误差（4.52）远低于 VideoComposer（15.41），证明了 MMCU 对空间结构的精准捕捉。
*   **解耦灵活性：** 证明了视觉和听觉控制可以独立调节，互不干扰，为个性化媒体创作提供了极大的自由度。

### 7. 优点（亮点）
*   **原生联合生成：** 不同于先生成视频再配音的方案，MMControl 在联合潜空间内操作，天然保证了音画同步。
*   **非侵入式设计：** 采用旁路分支（Bypass）而非全量微调，既保留了底座模型的强大生成能力，又降低了训练成本。
*   **多模态组合：** 首次实现了将身份、音色、姿态、深度等多种异构信号在同一个 DiT 框架下进行精细化组合控制。

### 8. 不足与局限
*   **场景复杂度限制：** 目前主要集中在单人/单角色场景，对于**多角色互动**或复杂对话场景的跨人称同步仍有待探索。
*   **泛化性风险：** 虽然在测试集表现良好，但对于完全未见过的极端语音风格或复杂的非人类动作，可能仍需进一步增强鲁棒性。
*   **长视频支持：** 论文展示多为短片段，长时序下的逻辑一致性和控制稳定性尚未深入讨论。

（完）
